import{pool}from'./db.js';const migrations=[{id:'001_core_evolution',sql:`ALTER TABLE tenants ADD COLUMN IF NOT EXISTS settings jsonb NOT NULL DEFAULT '{}'::jsonb;ALTER TABLE work_schedules ADD COLUMN IF NOT EXISTS schedule_type text NOT NULL DEFAULT 'WEEKLY';ALTER TABLE work_schedules ADD COLUMN IF NOT EXISTS cycle_days jsonb;CREATE TABLE IF NOT EXISTS employee_exceptions(id uuid PRIMARY KEY DEFAULT gen_random_uuid(),tenant_id uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,employee_id uuid NOT NULL REFERENCES employees(id) ON DELETE CASCADE,type text NOT NULL,starts_on date NOT NULL,ends_on date NOT NULL,reason text,minutes integer NOT NULL DEFAULT 0,created_by uuid REFERENCES users(id),created_at timestamptz NOT NULL DEFAULT now());CREATE TABLE IF NOT EXISTS holidays(id uuid PRIMARY KEY DEFAULT gen_random_uuid(),tenant_id uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,name text NOT NULL,holiday_date date NOT NULL,created_at timestamptz NOT NULL DEFAULT now(),UNIQUE(tenant_id,holiday_date));CREATE TABLE IF NOT EXISTS time_bank_adjustments(id uuid PRIMARY KEY DEFAULT gen_random_uuid(),tenant_id uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,employee_id uuid NOT NULL REFERENCES employees(id) ON DELETE CASCADE,adjustment_date date NOT NULL,minutes integer NOT NULL CHECK(minutes<>0),type text NOT NULL DEFAULT 'MANUAL',reason text NOT NULL,created_by uuid REFERENCES users(id),created_at timestamptz NOT NULL DEFAULT now());CREATE INDEX IF NOT EXISTS idx_exceptions_employee_dates ON employee_exceptions(tenant_id,employee_id,starts_on,ends_on);CREATE INDEX IF NOT EXISTS idx_bank_employee_date ON time_bank_adjustments(tenant_id,employee_id,adjustment_date DESC);`},{id:'002_period_closures',sql:`CREATE TABLE IF NOT EXISTS period_closures(id uuid PRIMARY KEY DEFAULT gen_random_uuid(),tenant_id uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,employee_id uuid NOT NULL REFERENCES employees(id) ON DELETE CASCADE,period char(7) NOT NULL,status text NOT NULL DEFAULT 'OPEN' CHECK(status IN('OPEN','CLOSED')),closed_by uuid REFERENCES users(id),closed_at timestamptz,created_at timestamptz NOT NULL DEFAULT now(),UNIQUE(tenant_id,employee_id,period));CREATE INDEX IF NOT EXISTS idx_period_closures_lookup ON period_closures(tenant_id,employee_id,period,status);`},{id:'003_payroll_preview_legacy',sql:`ALTER TABLE employees ADD COLUMN IF NOT EXISTS salary_base numeric(12,2) NOT NULL DEFAULT 0;ALTER TABLE employees ADD COLUMN IF NOT EXISTS monthly_hours numeric(7,2) NOT NULL DEFAULT 220;ALTER TABLE employees ADD COLUMN IF NOT EXISTS overtime_percent numeric(6,2) NOT NULL DEFAULT 50;ALTER TABLE employees ADD COLUMN IF NOT EXISTS absence_discount boolean NOT NULL DEFAULT true;CREATE TABLE IF NOT EXISTS payroll_event_rules(id uuid PRIMARY KEY DEFAULT gen_random_uuid(),tenant_id uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,name text NOT NULL,code text,event_type text NOT NULL CHECK(event_type IN('EARNING','DEDUCTION')),calculation_base text NOT NULL CHECK(calculation_base IN('FIXED','SALARY_PERCENT','HOUR_VALUE','OVERTIME_VALUE')),value numeric(12,4) NOT NULL DEFAULT 0,active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now());`},{id:'004_punch_race_guard',sql:`CREATE OR REPLACE FUNCTION cactus_punch_race_guard() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE last_at timestamptz; BEGIN PERFORM pg_advisory_xact_lock(hashtext(NEW.tenant_id::text||':'||NEW.employee_id::text)); IF NEW.collector='WEB_PWA' THEN SELECT occurred_at INTO last_at FROM punches WHERE tenant_id=NEW.tenant_id AND employee_id=NEW.employee_id AND collector='WEB_PWA' ORDER BY occurred_at DESC LIMIT 1; IF last_at IS NOT NULL AND NEW.occurred_at>=last_at AND NEW.occurred_at-last_at<interval '5 seconds' THEN RAISE EXCEPTION USING ERRCODE='P0001',MESSAGE='PUNCH_DUPLICATE_RACE'; END IF; END IF; RETURN NEW; END $$;DROP TRIGGER IF EXISTS trg_cactus_punch_race_guard ON punches;CREATE TRIGGER trg_cactus_punch_race_guard BEFORE INSERT ON punches FOR EACH ROW EXECUTE FUNCTION cactus_punch_race_guard();`},{id:'005_punch_policy_default',sql:`UPDATE tenants SET settings=jsonb_set(coalesce(settings,'{}'::jsonb),'{punchAnticipationMinutes}',coalesce(settings->'punchAnticipationMinutes','4'::jsonb),true);`},{id:'006_punch_idempotency',sql:`ALTER TABLE punches ADD COLUMN IF NOT EXISTS idempotency_key text;CREATE UNIQUE INDEX IF NOT EXISTS uq_punch_idempotency ON punches(tenant_id,employee_id,idempotency_key) WHERE idempotency_key IS NOT NULL;CREATE TABLE IF NOT EXISTS ledger_recalc_queue(id bigserial PRIMARY KEY,tenant_id uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,employee_id uuid NOT NULL REFERENCES employees(id) ON DELETE CASCADE,work_date date NOT NULL,status text NOT NULL DEFAULT 'PENDING' CHECK(status IN('PENDING','PROCESSING','DONE','FAILED')),attempts integer NOT NULL DEFAULT 0,last_error text,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(tenant_id,employee_id,work_date));CREATE INDEX IF NOT EXISTS idx_ledger_recalc_pending ON ledger_recalc_queue(status,created_at);`},{id:'007_employee_identity_uniqueness',sql:`UPDATE employees SET email=lower(trim(email)),registration=trim(registration),cpf=regexp_replace(cpf,'\D','','g');CREATE UNIQUE INDEX IF NOT EXISTS uq_employee_tenant_email_ci ON employees(tenant_id,lower(email)) WHERE email IS NOT NULL;CREATE UNIQUE INDEX IF NOT EXISTS uq_employee_tenant_registration_ci ON employees(tenant_id,lower(registration)) WHERE registration IS NOT NULL;CREATE UNIQUE INDEX IF NOT EXISTS uq_user_tenant_email_ci ON users(tenant_id,lower(email));`},{id:'008_database_integrity_guards',sql:`DO $$ BEGIN IF EXISTS(SELECT 1 FROM employees GROUP BY tenant_id,regexp_replace(cpf,'\D','','g') HAVING count(*)>1) THEN RAISE EXCEPTION 'Duplicate normalized CPF found; manual remediation required'; END IF; IF EXISTS(SELECT 1 FROM employees WHERE cpf!~'^[0-9]{11}$' OR registration IS NULL OR btrim(registration)='') THEN RAISE EXCEPTION 'Invalid employee identity found; manual remediation required'; END IF; END $$;CREATE UNIQUE INDEX IF NOT EXISTS uq_employee_tenant_cpf_normalized ON employees(tenant_id,(regexp_replace(cpf,'\D','','g')));ALTER TABLE employees DROP CONSTRAINT IF EXISTS chk_employee_cpf_normalized;ALTER TABLE employees ADD CONSTRAINT chk_employee_cpf_normalized CHECK(cpf~'^[0-9]{11}$');ALTER TABLE employees ALTER COLUMN registration SET NOT NULL;CREATE UNIQUE INDEX IF NOT EXISTS uq_employee_tenant_pair ON employees(tenant_id,id);CREATE UNIQUE INDEX IF NOT EXISTS uq_user_tenant_pair ON users(tenant_id,id);CREATE UNIQUE INDEX IF NOT EXISTS uq_schedule_tenant_pair ON work_schedules(tenant_id,id);ALTER TABLE punches DROP CONSTRAINT IF EXISTS fk_punch_employee_tenant;ALTER TABLE punches ADD CONSTRAINT fk_punch_employee_tenant FOREIGN KEY(tenant_id,employee_id) REFERENCES employees(tenant_id,id);ALTER TABLE daily_time_ledger DROP CONSTRAINT IF EXISTS fk_ledger_employee_tenant;ALTER TABLE daily_time_ledger ADD CONSTRAINT fk_ledger_employee_tenant FOREIGN KEY(tenant_id,employee_id) REFERENCES employees(tenant_id,id);ALTER TABLE adjustment_requests DROP CONSTRAINT IF EXISTS fk_adjustment_employee_tenant;ALTER TABLE adjustment_requests ADD CONSTRAINT fk_adjustment_employee_tenant FOREIGN KEY(tenant_id,employee_id) REFERENCES employees(tenant_id,id);ALTER TABLE time_bank_adjustments DROP CONSTRAINT IF EXISTS fk_bank_employee_tenant;ALTER TABLE time_bank_adjustments ADD CONSTRAINT fk_bank_employee_tenant FOREIGN KEY(tenant_id,employee_id) REFERENCES employees(tenant_id,id);ALTER TABLE employee_schedules DROP CONSTRAINT IF EXISTS fk_employee_schedule_employee_tenant;ALTER TABLE employee_schedules ADD CONSTRAINT fk_employee_schedule_employee_tenant FOREIGN KEY(tenant_id,employee_id) REFERENCES employees(tenant_id,id) ON DELETE CASCADE;ALTER TABLE employee_schedules DROP CONSTRAINT IF EXISTS fk_employee_schedule_schedule_tenant;ALTER TABLE employee_schedules ADD CONSTRAINT fk_employee_schedule_schedule_tenant FOREIGN KEY(tenant_id,schedule_id) REFERENCES work_schedules(tenant_id,id);CREATE OR REPLACE FUNCTION cactus_punch_race_guard() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE last_at timestamptz; BEGIN PERFORM pg_advisory_xact_lock(hashtext(NEW.tenant_id::text||':'||NEW.employee_id::text)); IF NEW.collector='WEB_PWA' THEN SELECT occurred_at INTO last_at FROM punches WHERE tenant_id=NEW.tenant_id AND employee_id=NEW.employee_id AND collector='WEB_PWA' ORDER BY occurred_at DESC LIMIT 1; IF last_at IS NOT NULL AND NEW.occurred_at>=last_at AND NEW.occurred_at-last_at<interval '5 seconds' THEN RAISE EXCEPTION USING ERRCODE='P0001',MESSAGE='PUNCH_DUPLICATE_RACE'; END IF; END IF; RETURN NEW; END $$;`},{id:'009_tenant_domains',sql:`CREATE TABLE IF NOT EXISTS tenant_domains(id uuid PRIMARY KEY DEFAULT gen_random_uuid(),tenant_id uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,domain text NOT NULL,type text NOT NULL CHECK(type IN('CACTUS','CUSTOM')),verified boolean NOT NULL DEFAULT false,is_primary boolean NOT NULL DEFAULT false,verification_token text,verified_at timestamptz,created_at timestamptz NOT NULL DEFAULT now());CREATE UNIQUE INDEX IF NOT EXISTS uq_tenant_domains_domain_ci ON tenant_domains(lower(domain));CREATE INDEX IF NOT EXISTS idx_tenant_domains_tenant ON tenant_domains(tenant_id,type,verified);CREATE UNIQUE INDEX IF NOT EXISTS uq_tenant_domains_primary ON tenant_domains(tenant_id) WHERE is_primary;`}];export async function migrate(){const c=await pool.connect();try{await c.query(`CREATE TABLE IF NOT EXISTS schema_migrations(id text PRIMARY KEY,applied_at timestamptz NOT NULL DEFAULT now())`);await c.query(`SELECT pg_advisory_lock(hashtext('cactus-ponto:migrations'))`);for(const m of migrations){const exists=await c.query(`SELECT 1 FROM schema_migrations WHERE id=$1`,[m.id]);if(exists.rowCount)continue;console.log(`[migration] applying ${m.id}`);await c.query('BEGIN');try{await c.query(m.sql);await c.query(`INSERT INTO schema_migrations(id)VALUES($1)`,[m.id]);await c.query('COMMIT');console.log(`[migration] applied ${m.id}`)}catch(e){await c.query('ROLLBACK');throw new Error(`Migration ${m.id} failed: ${e.message}`)}}}finally{try{await c.query(`SELECT pg_advisory_unlock(hashtext('cactus-ponto:migrations'))`)}catch{}c.release()}}export const migrationIds=migrations.map(x=>x.id);