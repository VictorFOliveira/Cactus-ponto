import{query}from'./db.js';export const monthOf=d=>String(d||'').slice(0,7);export async function isPeriodClosed(tenantId,employeeId,date,client=null){const q=client?.query?client.query.bind(client):query,r=await q(`SELECT 1 FROM period_closures WHERE tenant_id=$1 AND employee_id=$2 AND period=$3 AND status='CLOSED' LIMIT 1`,[tenantId,employeeId,monthOf(date)]);return r.rowCount>0}export async function assertPeriodOpen(tenantId,employeeId,date,client=null){if(await isPeriodClosed(tenantId,employeeId,date,client)){const e=new Error(`Competência ${monthOf(date)} fechada. Reabra o período antes de alterar dados de ponto.`);e.status=423;throw e}}