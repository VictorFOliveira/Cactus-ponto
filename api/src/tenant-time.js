import{query}from'./db.js';export async function tenantTimezone(tenantId,client=null){const q=client?.query?client.query.bind(client):query,r=await q(`SELECT timezone FROM tenants WHERE id=$1`,[tenantId]);return r.rows[0]?.timezone||'America/Sao_Paulo'}export function localParts(date=new Date(),timeZone='America/Sao_Paulo'){const p=new Intl.DateTimeFormat('en-CA',{timeZone,year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',second:'2-digit',hourCycle:'h23'}).formatToParts(date).reduce((a,x)=>(a[x.type]=x.value,a),{});return{date:`${p.year}-${p.month}-${p.day}`,time:`${p.hour}:${p.minute}:${p.second}`,hour:+p.hour,minute:+p.minute,minuteOfDay:+p.hour*60 + +p.minute}}export async function tenantNow(tenantId,client=null,date=new Date()){const timezone=await tenantTimezone(tenantId,client);return{timezone,...localParts(date,timezone)}}export const localDate=(date,timeZone)=>localParts(date,timeZone).date;export const localMinute=(date,timeZone)=>localParts(date,timeZone).minuteOfDay;