import pg from'pg';const{Pool}=pg;export const pool=new Pool({connectionString:process.env.DATABASE_URL,options:'-c timezone=UTC'});pool.on('connect',client=>client.query(`SET TIME ZONE 'UTC'`).catch(e=>console.error('Failed to set DB timezone',e)));export const query=(text,params=[])=>pool.query(text,params);export async function health(){const r=await query(`select now() as now,current_setting('TIMEZONE') timezone`);return r.rows[0]}