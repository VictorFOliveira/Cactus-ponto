import crypto from'crypto';import{pool}from'./db.js';import{advisoryLock}from'./security.js';import{calculateDay}from'./time-engine.js';const order={ENTRY:'BREAK_START',BREAK_START:'BREAK_END',BREAK_END:'EXIT',EXIT:'EXTRA',EXTRA:'EXTRA'};export async function registerPunch(user){if(!user.employeeId){const e=new Error('Usuário sem colaborador vinculado');e.status=400;throw e}const c=await pool.connect();let row,date;try{await c.query('BEGIN');await advisoryLock(c,'punch',user.tenantId,user.employeeId);const active=await c.query(`SELECT active FROM employees WHERE tenant_id=$1 AND id=$2 FOR UPDATE`,[user.tenantId,user.employeeId]);if(!active.rowCount||!active.rows[0].active){const e=new Error('Colaborador inativo ou não encontrado');e.status=403;throw e}const now=new Date();date=now.toISOString().slice(0,10);const last=await c.query(`SELECT type,occurred_at FROM punches WHERE tenant_id=$1 AND employee_id=$2 AND occurred_at::date=$3::date AND type<>'ADJUSTMENT' ORDER BY occurred_at DESC LIMIT 1`,[user.tenantId,user.employeeId,date]);if(last.rowCount){const elapsed=now-new Date(last.rows[0].occurred_at);if(elapsed<5000){const e=new Error('Ponto já registrado há poucos segundos');e.status=409;throw e}}const type=last.rows[0]?order[last.rows[0].type]:'ENTRY',nr=await c.query(`SELECT nextval('punch_nsr_seq') nsr`),nsr=nr.rows[0].nsr,hash=crypto.createHash('sha256').update(`${user.tenantId}|${user.employeeId}|${now.toISOString()}|${nsr}|${type}`).digest('hex'),r=await c.query(`INSERT INTO punches(tenant_id,nsr,employee_id,type,collector,occurred_at,hash)VALUES($1,$2,$3,$4,'WEB_PWA',$5,$6)RETURNING *,occurred_at at`,[user.tenantId,nsr,user.employeeId,type,now,hash]);row=r.rows[0];await c.query('COMMIT')}catch(e){await c.query('ROLLBACK');throw e}finally{c.release()}await calculateDay(user.tenantId,user.employeeId,date);return row}