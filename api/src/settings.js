import{Router}from'express';import{query}from'./db.js';const router=Router();export function settingsRoutes(auth){router.get('/settings',auth(['ADMIN','HR']),async(req,res,next)=>{try{const[t,h]=await Promise.all([query(`SELECT id,name,trade_name,cnpj,timezone,coalesce(settings,'{}'::jsonb)settings FROM tenants WHERE id=$1`,[req.user.tenantId]),query(`SELECT * FROM holidays WHERE tenant_id=$1 ORDER BY holiday_date`,[req.user.tenantId])]);res.json({...t.rows[0],holidays:h.rows})}catch(e){next(e)}});router.put('/settings',auth(['ADMIN','HR']),async(req,res,next)=>{try{const{timezone,settings={}}=req.body,r=await query(`UPDATE tenants SET timezone=coalesce($2,timezone),settings=coalesce(settings,'{}'::jsonb)||$3::jsonb WHERE id=$1 RETURNING timezone,settings`,[req.user.tenantId,timezone||null,JSON.stringify(settings)]);res.json(r.rows[0])}catch(e){next(e)}});router.post('/holidays',auth(['ADMIN','HR']),async(req,res,next)=>{try{const{name,date}=req.body;if(!name||!date)return res.status(400).json({error:'Nome e data são obrigatórios'});const r=await query(`INSERT INTO holidays(tenant_id,name,holiday_date)VALUES($1,$2,$3) ON CONFLICT(tenant_id,holiday_date)DO UPDATE SET name=$2 RETURNING *`,[req.user.tenantId,name,date]);res.status(201).json(r.rows[0])}catch(e){next(e)}});router.delete('/holidays/:id',auth(['ADMIN','HR']),async(req,res,next)=>{try{await query(`DELETE FROM holidays WHERE tenant_id=$1 AND id=$2`,[req.user.tenantId,req.params.id]);res.status(204).end()}catch(e){next(e)}});return router}