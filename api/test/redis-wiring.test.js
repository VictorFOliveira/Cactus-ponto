import test from'node:test';import assert from'node:assert/strict';import fs from'node:fs';const compose=fs.readFileSync(new URL('../../docker-compose.yml',import.meta.url),'utf8'),server=fs.readFileSync(new URL('../src/server.js',import.meta.url),'utf8'),domains=fs.readFileSync(new URL('../src/tenant-domains.js',import.meta.url),'utf8');test('compose keeps redis internal and wires API dependency',()=>{assert.match(compose,/redis:\n    image: redis:7-alpine/);const block=compose.slice(compose.indexOf('  redis:'),compose.indexOf('  api:'));assert.doesNotMatch(block,/ports:/);assert.match(compose,/REDIS_URL/)});test('only low-risk read models are cached',()=>{assert.match(server,/branding:/);assert.match(server,/dashboard:/);assert.match(domains,/domain:/);assert.doesNotMatch(server,/cacheSet\(`punch/);assert.doesNotMatch(server,/cacheSet\(`privacy/)});