import test from'node:test';import assert from'node:assert/strict';import{formatMinutes}from'../src/closing.js';test('closing CSV formats positive and negative minute balances without floor distortion',()=>{assert.equal(formatMinutes(0),'0:00');assert.equal(formatMinutes(30),'0:30');assert.equal(formatMinutes(90),'1:30');assert.equal(formatMinutes(-30),'-0:30');assert.equal(formatMinutes(-90),'-1:30');assert.equal(formatMinutes(-61),'-1:01')});